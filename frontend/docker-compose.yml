services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '${PORT:-4173}:4173'
    env_file:
      - .env
    environment:
      PORT: ${PORT:-4173}
